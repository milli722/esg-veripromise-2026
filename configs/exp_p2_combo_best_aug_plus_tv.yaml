extends: exp_p2_combo_best_aug_plus.yaml
exp_name: p2_combo_best_aug_plus_tv
# Phase 41 — retrain on combined train+val (≈2000 rows) for final submission.
# Aug-Plus v1 + U10 v2 pseudo + U6-pro BT pool, class-weighted + Focal γ=3.0 (T4).
seeds: [42]
data:
  csv_path: data/processed/train_val_combined.csv
